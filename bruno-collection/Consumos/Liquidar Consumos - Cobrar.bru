meta {
  name: Liquidar Consumos - Cobrar
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/consumos/liquidar
  body: json
  auth: none
}

body:json {
  {
    "consumoIds": ["{{consumoId}}"],
    "reglaAplicada": "precio_venta",
    "motivo": "Liquidación mensual"
  }
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response has success", function() {
    expect(res.getBody().success).to.equal(true);
  });
}
