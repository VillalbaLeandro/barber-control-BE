meta {
  name: Historial de Consumos por Staff
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/consumos/staff/{{staffId}}?limit=20&offset=0
  body: none
  auth: none
}

params:query {
  limit: 20
  offset: 0
  ~estado: pendiente
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response is an array", function() {
    expect(res.getBody()).to.be.an('array');
  });
}
